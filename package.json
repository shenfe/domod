{
    "scripts": {
        "build:OArray": "./node_modules/.bin/rollup -c ./rollup-OArray.config.js",
        "build:Kernel": "./node_modules/.bin/rollup -c ./rollup-Kernel.config.js",
        "build:min": "./node_modules/.bin/rollup -c ./rollup-min.config.js",
        "build": "./node_modules/.bin/rollup -c ./rollup.config.js",
        "dev": "./node_modules/.bin/rollup -w -c ./rollup.config.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "rollup": "^0.46.0",
        "rollup-plugin-uglify": "^2.0.1",
        "source-map-support": "^0.4.15"
    }
}
